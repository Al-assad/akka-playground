akka {

  // akka-http
  http {
    host-connection-pool.client.idle-timeout = 5s
    server {
      idle-timeout = 60s
    }
    client {
      connection-timeout = 5s
      idle-timeout = 60s
    }
  }

}
